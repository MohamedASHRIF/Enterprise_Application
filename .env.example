# Example environment variables for docker-compose (do NOT commit secrets)
# Copy this file to .env and fill in real values before running `docker compose up`.

# Gemini / external LLM
GEMINI_API_URL=
GEMINI_API_KEY=

# Optional override for customer service URL (defaults to the compose service)
CUSTOMER_SERVICE_URL=http://customer_service:8085
