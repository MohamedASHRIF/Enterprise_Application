server.port=8080
spring.jackson.serialization.indent_output=true

# Gemini/LLM configuration - do NOT commit secrets here.
# The service reads GEMINI_API_URL and GEMINI_API_KEY from the environment.
# When running locally with docker-compose, set these via the compose file or a .env file.

customer.service.url: "http://localhost:8085"
